Username: {{{post.author.username}}}
Post created at: {{{post.createdAt}}}
Post title: {{{post.title}}}
Post content: {{{post.content}}}
<br>
{{{error}}}

<form class="form" method="post" action="/posts/{{post._id}}/likes/">
{{likeError}}
  <div class="field">
    <label class="label">Username:</label>
    <div class="control">
      <input class="input" type="text" name="username">
    </div>
  </div>
  <div class="control">
    <button type="submit">Like this post</button>
  </div>
</form>

<form class="form" method="post" action="/posts/{{post._id}}/comments/">
  {{{commentError}}}
  <div class="field">
    <label class="label">Username:</label>
    <div class="control">
      <input class="input" type="text" name="username">
    </div>
  </div>

  <div class="field">
    <label class="label">Content:</label>
    <div class="control">
      <input class="input" type="text" name="content">
    </div>
  </div>

  <div class="control">
    <button type="submit">Submit</button>
  </div>
</form>
<br>
Likes ({{{post.likes.length}}}): <br>
{{#each post.likes}}
Username: {{username}}
{{/each}}
<br>
Comments:
<br>
{{#each comments}}
Username: {{author.username}}
Content: {{content}}
{{/each}}